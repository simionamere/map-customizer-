<?xml version="1.0" encoding="utf-8"?>
<!--
  Place this in:  android/app/src/main/res/values/splash.xml
  (Replace or merge with your existing splash.xml)

  windowSplashScreenAnimatedIcon  → your animated icon
  windowSplashScreenBackground    → white background
  windowSplashScreenIconSize      → 45% of screen width is handled
                                     in SplashActivity via code
  postSplashScreenTheme           → hands off to your normal AppTheme
-->
<resources>
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_animated</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
